node_modules
dist
*.local
.DS_Store
# Regenerated on every CI run (npm run scrape / scrape:favorites) — not versioned.
public/catalog.json
public/favorites.json
# venues.json (coords des théâtres) est quasi statique et versionné : le CI ne le
# régénère pas (npm run scrape:venues à lancer à la main entre éditions).
