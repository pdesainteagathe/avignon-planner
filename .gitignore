node_modules
dist
*.local
.DS_Store
# Generated artifacts (regenerate with `npm run scrape` / `npm run scrape:favorites`).
# Commit them only when you want the deployed site to ship with data baked in.
public/catalog.json
public/favorites.json
