node_modules
dist
*.local
.DS_Store
# Scraped catalog is a generated artifact (regenerate with `npm run scrape`).
# Commit it only when you want the deployed site to ship with data baked in.
public/catalog.json
